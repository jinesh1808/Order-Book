{
  "name": "order-book",
  "version": "1.0.0",
  "description": "Full stack synthetic order book",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
